[Unit]
Description=WeazyStroke gesture daemon
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/eswl-daemon --overlay --tray --grab
Restart=on-failure
RestartSec=2
KillMode=process

[Install]
WantedBy=graphical-session.target
